# oeradio.at MCP Server - Environment Variables
# Kopieren zu .env und anpassen

# Server Port (default: 3000)
PORT=3000

# Cloudflare Tunnel Token (optional, für öffentlichen Zugriff)
# Erstellen mit: cloudflared tunnel create oeradio-mcp
# Token finden mit: cloudflared tunnel token oeradio-mcp
TUNNEL_TOKEN=
